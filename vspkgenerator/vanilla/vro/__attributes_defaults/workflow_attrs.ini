[all]
includes=name

[DHCPOption]
includes=length, value

[DSCPForwardingClassMapping]
includes=DSCP, forwardingClass

[EgressACLEntryTemplate]
includes=destinationPort, networkType, protocol, sourcePort

[EgressQOSPolicy]
includes=parentQueueAssociatedRateLimiterID, queue1AssociatedRateLimiterID, queue1ForwardingClasses, queue2AssociatedRateLimiterID, queue2ForwardingClasses, queue3AssociatedRateLimiterID, queue3ForwardingClasses, queue4AssociatedRateLimiterID, queue4ForwardingClasses

[Gateway]
includes=systemID

[IKEGatewayProfile]
includes=associatedIKEGatewayID, IKEGatewayIdentifier, IKEGatewayIdentifierType

[IKEPSK]
includes=unencryptedPSK

[InfrastructureGatewayProfile]
includes=deadTimer, deadTimerEnabled

[InfrastructurePortProfile]
includes=mtu, duplex, speed, uplinkTag

[InfrastructureVscProfile]
includes=firstController, probeInterval, secondController

[IngressACLEntryTemplate]
includes=destinationPort, networkType, protocol, sourcePort

[IngressAdvFwdTemplate]
includes=active, priority, priorityType

[IngressAdvFwdEntryTemplate]
includes=destinationPort, networkType, protocol, sourcePort

[IngressExternalServiceTemplate]
includes=active, priorityType

[IngressExternalServiceTemplateEntry]
includes=destinationPort, networkType, protocol, sourcePort

[L2Domain]
includes=templateID

[MirrorDestination]
includes=destinationIp

[NSRedundantGatewayGroup]
includes=gatewayPeer1ID, gatewayPeer2ID

[PATNATPool]
includes=defaultPATIP, endAddressRange, startAddressRange

[RedundancyGroup]
includes=gatewayPeer1ID, gatewayPeer2ID

[QOS]
includes=active, rateLimitingActive, peak, burst, serviceClass

[RateLimiter]
includes=committedInformationRate, peakBurstSize, peakInformationRate

[SiteInfo]
includes=siteIdentifier

[Subnet]
includes=address, gateway, netmask

[SubnetTemplate]
includes=gateway, proxyARP, splitSubnet

[VPNConnection]
includes=associatedWANServiceID

[WANService]
includes=WANServiceIdentifier

[Zone]
includes=maintenanceMode